# Publish — RiskManager Library

This folder contains submission-related files for the RiskManager library.

Libraries are typically bundled with tools that depend on them rather than sold separately.

**Description**: Position sizing, drawdown limits, daily loss caps
**Version**: v0.0.1
**ML modules**: None (utility library)
